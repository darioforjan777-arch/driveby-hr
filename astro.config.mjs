import { defineConfig } from 'astro/config';

export default defineConfig({
  // TODO: promijeni na pravu domenu (npr. https://driveby.hr) čim je Dario kupi i poveže na Vercel.
  site: 'https://driveby-hr.vercel.app',
  compressHTML: true,
});
